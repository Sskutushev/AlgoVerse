feat: Finalize main page and deploy to GitHub Pages

This commit summarizes the work done in this session, including the deployment.

Key changes include:
- Updated `quotesData` in `src/App.js` with new financial symbols and data.
- Adjusted UI elements on the landing page (`min-height` and image height).
- Refactored `src/pages/InstrumentDetailsPage.js` to a two-column layout with detailed financial metrics and news.
- Performed `npm run build` and `gh-pages -d build` for deployment to https://sskutushev.github.io/AlgoVerse/.
- Updated `мастер-рабочей-сессии.txt` with session summary.